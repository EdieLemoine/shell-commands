#!/usr/bin/env bash

test -e "${HOME}/.zsh/deleteBranches.zsh" && source "${HOME}/.zsh/deleteBranches.zsh"
test -e "${HOME}/.zsh/deleteBranch.zsh" && source "${HOME}/.zsh/deleteBranch.zsh"
test -e "${HOME}/.zsh/tagcommand.zsh" && source "${HOME}/.zsh/tagcommand.zsh"
test -e "${HOME}/.zsh/replaceTag.zsh" && source "${HOME}/.zsh/replaceTag.zsh"
